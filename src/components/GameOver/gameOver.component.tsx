import React, { FC } from 'react';

// TODO: Show a score breakdown and winner
const GameOver: FC = () => {
    return (
        <div>
            <p>Hi, I'm the Game Over component.</p>
        </div>
    );
};

export default GameOver;
